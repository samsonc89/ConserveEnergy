---
import { Image } from "astro:assets";
import phone from "../images/phone.svg";
import Button from "./Button.astro";
---

<section class="rounded-3xl shadow-2xl max-lg:mx-4">
  <div
    class="flex justify-center py-12 px-20 items-center max-lg:px-6 max-sm:hidden"
  >
    <div class="content-wrapper flex flex-col items-start gap-6 flex-1">
      <p class="caption uppercase">for a free consultation</p>
      <h2 class="uppercase w-3/4 max-lg:w-full">
        looking at the bigger picture
      </h2>
      <p class="body xl:w-10/12 mb-4">
        By looking at your big picture, we find the cost-effective pathway to
        making the energy saving measures reality. Conserve Energy focuses
        solely on your energy needs, and how to trim your energy use. We provide
        the in-house expertise to be your turnkey solution for total energy
        efficiency.
      </p>
      <p class="body xl:w-10/12">
        Our experienced network of suppliers and subcontractors lets us specify
        and deliver the very best to you. Expect cutting-edge products at
        competitive prices when you work with Conserve Energy.
      </p>
      <a href="/ConserveEnergy">
        <Button class="mt-10" id="scheduleBtn" theme="button">
          Schedule Now
        </Button>
      </a>
    </div>
    <div class="img-wrapper flex justify-center items-center max-lg:hidden">
      <Image src={phone} alt="phone" />
    </div>
  </div>

  <div
    class="justify-center py-12 px-20 items-center max-lg:px-6 max-sm:block hidden"
  >
    <div class="form-wrapper flex-1">
      <div class="mb-6">
        <div class="caption mb-6">Contact</div>

        <h2 class="mb-6">Feel free to contact us anytime</h2>

        <p class="mb-6">
          Fill in the information below and we will contact you. Let's see if we
          will start a new project. Don't hesitate to contact us.
        </p>
      </div>

      <div class="flex-col mt-6">
        <form method="POST">
          <label>
            <input
              type="text"
              name="username"
              placeholder="Name"
              class="border border-color: text-title-color py-3 pl-4 pr-8 rounded-lg w-full mb-8"
              required
            />
          </label>
          <label>
            <input
              type="tel"
              name="phone"
              placeholder="Phone"
              class="border border-color: text-title-color py-3 pl-4 pr-8 rounded-lg w-full mb-8"
              required
            />
          </label>
          <label>
            <label>
              <input
                type="email"
                name="email"
                placeholder="Email"
                class="border border-color: text-title-color py-3 pl-4 pr-8 rounded-lg mb-8 w-full"
                required
              />
            </label>
            <a href="/ConserveEnergy/contact">
              <Button
                class="w-full flex justify-center"
                id="scheduleBtn"
                theme="button"
              >
                Submit
              </Button>
            </a>
          </label>
        </form>
      </div>
    </div>
  </div>
</section>

<style>
  /* * {
border: 1px solid red;
  } */

  section {
    background-color: var(--accent-color);
  }
</style>
